# Linux & Bash Automation for DevOps Operations

## Description
Created Bash scripts for log cleanup, disk monitoring, and service checks. Improves operational efficiency through basic automation.

## Technologies
- Linux
- Bash Scripting

## Usage
- Run `./log_cleanup.sh` for logs.
- Run `./disk_monitor.sh` for disk.
- Run `./service_check.sh` for services.
- Run `./devops_automation.sh` for all.

## Github Comments
- git init
- git config --global user.name "Vasan104"
- git config --global user.email "thirumalaisrinivas104@gmail.com"
- git status
- git add .
- git status
- git commit -m "Completed the Project Linux & Bash Automation"
- git branch -M main
- git remote add origin https://github.com/Vasan104/linux-bash-automation.git
- git push -u origin main
